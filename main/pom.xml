<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>com.compilit</groupId>
    <artifactId>hiberate-or-hibernot</artifactId>
    <version>1.0-SNAPSHOT</version>
  </parent>

  <artifactId>main</artifactId>

  <dependencies>
    <dependency>
      <groupId>com.compilit</groupId>
      <artifactId>presentation</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>com.compilit</groupId>
      <artifactId>application</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>com.compilit</groupId>
      <artifactId>infrastructure</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <version>${spring-boot.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>${testcontainers-junit-juputer.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <version>${spring-boot.version}</version>
        <configuration>
          <mainClass>com.compilit.ToHibernateOrNot</mainClass>
        </configuration>
        <executions>
          <execution>
            <goals>
              <goal>repackage</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <!-- Records CPU/memory/GC behaviour of the regression test JVM. One .jfr per
               domain module, so runs across profiles (spring-boot-data-jpa vs spring-data-jdbc
               vs none) can be compared. Inspect with `jfr summary` or `jfr print`, or open in
               JDK Mission Control. -->
          <argLine>-XX:StartFlightRecording=filename=${project.build.directory}/regression-${domain.module}.jfr,settings=profile</argLine>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <profile>
      <id>spring-boot-data-jpa</id>
      <properties>
        <domain.module>spring-boot-data-jpa</domain.module>
      </properties>
    </profile>
    <profile>
      <id>spring-data-jdbc</id>
      <activation>
        <activeByDefault>true</activeByDefault>
      </activation>
      <properties>
        <domain.module>spring-data-jdbc</domain.module>
      </properties>
    </profile>
    <profile>
      <id>none</id>
      <properties>
        <domain.module>none</domain.module>
      </properties>
    </profile>
  </profiles>
</project>